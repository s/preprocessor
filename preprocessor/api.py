import sys
from .preprocessor import Preprocessor

p = Preprocessor()

def clean(tweet_string):
    matches = p.find_all(tweet_string)

def tokenize(tweet_string):
    matches = p.find_all(tweet_string)

def parse(tweet_string):
    matches = p.find_all(tweet_string)